
<p>This is my View: for ChangeSupplier</p>
<p>
    <br>
    @*@model FpisNada.Models.Supplier 
    @{<br>
    ViewBag.Title = "Index " ;
</p>
    <p>
        Layout = null;<br>
        }
    </p>
    
        @using (Html.BeginForm()) 
            {<br>
            @Html.AntiForgeryToken()<br>
            @Html.ValidationSummary(true)
         
        <p>@Html.TextBoxFor(model => model. SupplierID )</p>
        
            
            @if (ViewBag.ListaMesta != null) 
    {<br>
    @Html.DropDownListFor(m =>m.TownID , ViewBag.ListaMesta as SelectList,  "--select mesto-- " )
</p>
<p>}</p>
<p>@Html.DropDownListFor(m => m. StreetID , new SelectList("a","b" ),"--select ulicu--" )</p>
<p>
    <br>
    &lt;div class=&quot;container&quot;&gt;
</p>
<p>*@
    @*<br>
    @Html.TextBoxFor(model=> model. NumberAdress  )<br>
    @Html.TextBoxFor(model=> model.Email )<br>
    @Html.TextBoxFor(model=> model. NameSupplier )<br>
    @Html.TextBoxFor(model=> model.NumberOfPhone )<br>*@
    &lt;/div&gt;<br>
    &lt;/div&gt;<br>
    &lt;input type=&quot;submit&quot; value=&quot;Edit&quot; /&gt;<br>
    <input type="checkbox" name="b" value="u1" />
    <input type="checkbox" name="b" value="2" checked="checked" />
    <input type="checkbox" name="b" value="3" checked="checked" />
    <input type="button" id="submit" value="Click ME" />
    @section scripts{
        <script>


            $('#submit').on('click', function () {
                var STRMS = [];
                $('input:checked').each(function () {

                    STRMS.push($(this).attr("value"));
                });
                //$.ajax({
                //    url: "/MVC0405/Submit",
                //    type: "POST",
                //    data: { STRMSelected: STRMS },
                //    dataType: "json",
                //    traditional: true,
                //    success: function () {
                //        alert("ajax request to server succeed");
                //    }
                //});
                $.ajax({
                    url: "/MVC0405/Submit",
                    type: "POST",
                    data: { "Name":"abc" },
                    dataType: "json",
                    traditional: true,
                    success: function () {
                        alert("ajax request to server succeed");
                    }
                });
            });
        </script>
    }
