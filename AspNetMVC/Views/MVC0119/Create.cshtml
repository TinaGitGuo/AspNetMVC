@model AspNetMVC.BookMaster

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>BookMaster</h4>
        <hr />

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.strBookTypeId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.strBookTypeId, new { @class = "form-control" })

                @* <input type="text" id="strBookTypeId" name="strBookTypeId" class="form-control"  />*@
                @Html.ValidationMessageFor(model => model.strBookTypeId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.strAccessionId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.strAccessionId, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.strAccessionId, "", new { @class = "text-danger" })
            </div>
        </div>
        @*<input type="text" id="abc"  />*@
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}  
@*<button type="button" class="btn btn-lg btn-danger " data-toggle="popover" data-content="And here's some amazing content. It's very engaging. Right?" >Click to toggle popover</button>*@

    @*<script src="~/Content/JavaScript.js"></script>*@
@section scripts {
<script src="~/Scripts/jquery-1.10.2.js"></script><script src="~/Scripts/jquery.validate.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
   
@*<script src="http://static.runoob.com/assets/jquery-validation-1.14.0/lib/jquery.js"></script>
<script src="http://static.runoob.com/assets/jquery-validation-1.14.0/dist/jquery.validate.min.js"></script>*@

<script src="~/Scripts/bootstrap.min.js"></script>
<script>
     
    (function ($) {
        $(document).ready(function (e) {
            //The custom event caused by listening to the appendTo event in jquery
            $(".field-validation-valid").on('appendTochange', function (e) {
                var errText = $(this).children().eq(0).text();
                if (errText.length > 0) {
                    //alert(errText);  // Able to succeed in obtaining error information
                    //alert(this);

                    $(this).hide();   //Hide error messages
                    //$(this).parent().children
                    //$(this).attr("valmsg-for")
                    $('#' + $(this).parent().children().eq(0).attr("id")).attr("data-content", errText).popover("show")
                    //alert(errText);
                     

                    //$(this).parent().children('.validImg').attr('title', errText).show();             
                }
            });

            //
            $(".field-validation-valid").on('removeDatachange', function (e) {
                //maybe you want to do something
                //$('#' + $(this).parent().children().eq(0).attr("id")).attr("data-content", errText).popover("hide")
                //$('#strAccessionId').attr("data-content", errText).popover("hide")
                //alert("remow")
                //At this time the verification has been passed 
                //$(this).parent().children('.validImg').hide();
            });

        });
        $.each(["appendTo", "removeData"], function (i, methodname) {
            var oldmethod = $.fn[methodname];
            $.fn[methodname] = function () {
                oldmethod.apply(this, arguments);
                this.trigger(methodname + "change");
                return this;
            }
        });
    })(jQuery);

   
    </script>
   }


